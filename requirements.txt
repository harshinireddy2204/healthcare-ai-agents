# Core LLM & agent frameworks
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
crewai>=0.80.0
crewai-tools>=0.14.0

# Observability
langsmith>=0.1.0

# Orchestration
prefect>=3.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Frontend
streamlit>=1.38.0

# Data & storage
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# RAG — Vector store only (no local embedding models)
# We use OpenAI text-embedding-3-small via the openai SDK instead of
# local sentence-transformers. This saves ~700 MB RAM on Railway.
chromadb>=0.5.0

# OpenAI SDK — used for LLMs AND embeddings
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.5.0
rich>=13.8.0

# Knowledge Graph
networkx>=3.3

# Dev / testing
pytest>=8.3.0
pytest-asyncio>=0.23.0

# REMOVED to stay under Railway's 1 GB RAM cap:
#   torch                   (~300 MB)
#   sentence-transformers   (~90 MB + pulls transformers)
#   transformers            (~200 MB)
# These were used for local embeddings and cross-encoder reranking.
# We now use OpenAI's embedding API instead — zero local footprint.